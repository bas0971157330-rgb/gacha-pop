# Image URLs

Gacha Pop image fields can use full external URLs for product covers, galleries, banners, and popup ads.

If an image URL is empty or fails to load, the UI falls back to `/hero-machine.png` so product cards and previews do not show a broken image.
